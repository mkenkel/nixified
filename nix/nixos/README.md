## Noting manually-defined fixes here:

> [!NOTE]
> The following language servers were linked using the following to avoid dynamic linking issues reported by Neovim:

> ln -sf $(which <language-server>)  <language-server>

 - lua-language-server
 - nimlangserver
